### Animus Strategy Suggestions

**GOAP/scripts/Animus_Action/Animus_Action.gml** -> `timed`
<details><summary>Suggested template code</summary>

```gml
return Animus_StrategyTemplates.timed({
    expected_duration: (Animus_Core.is_callable(self.get_expected_duration) ? self.get_expected_duration() : 0.6),
    timeout: undefined,
    on_start: function(ctx) { if (Animus_Core.is_callable(self.start)) self.start(ctx); },
    on_stop:  function(ctx, reason) { if (Animus_Core.is_callable(self.stop)) self.stop(ctx, reason); }
});
```
</details>

**GOAP/scripts/Animus_ActionStrategy/Animus_ActionStrategy.gml** -> `timed`
<details><summary>Suggested template code</summary>

```gml
return Animus_StrategyTemplates.timed({
    expected_duration: (Animus_Core.is_callable(self.get_expected_duration) ? self.get_expected_duration() : 0.6),
    timeout: undefined,
    on_start: function(ctx) { if (Animus_Core.is_callable(self.start)) self.start(ctx); },
    on_stop:  function(ctx, reason) { if (Animus_Core.is_callable(self.stop)) self.stop(ctx, reason); }
});
```
</details>

**GOAP/scripts/Animus_Agent/Animus_Agent.gml** -> `timed`
<details><summary>Suggested template code</summary>

```gml
return Animus_StrategyTemplates.timed({
    expected_duration: (Animus_Core.is_callable(self.get_expected_duration) ? self.get_expected_duration() : 0.6),
    timeout: undefined,
    on_start: function(ctx) { if (Animus_Core.is_callable(self.start)) self.start(ctx); },
    on_stop:  function(ctx, reason) { if (Animus_Core.is_callable(self.stop)) self.stop(ctx, reason); }
});
```
</details>

**GOAP/scripts/Animus_Belief/Animus_Belief.gml** -> `timed`
<details><summary>Suggested template code</summary>

```gml
return Animus_StrategyTemplates.timed({
    expected_duration: (Animus_Core.is_callable(self.get_expected_duration) ? self.get_expected_duration() : 0.6),
    timeout: undefined,
    on_start: function(ctx) { if (Animus_Core.is_callable(self.start)) self.start(ctx); },
    on_stop:  function(ctx, reason) { if (Animus_Core.is_callable(self.stop)) self.stop(ctx, reason); }
});
```
</details>

**GOAP/scripts/Animus_Core/Animus_Core.gml** -> `timed`
<details><summary>Suggested template code</summary>

```gml
return Animus_StrategyTemplates.timed({
    expected_duration: (Animus_Core.is_callable(self.get_expected_duration) ? self.get_expected_duration() : 0.6),
    timeout: undefined,
    on_start: function(ctx) { if (Animus_Core.is_callable(self.start)) self.start(ctx); },
    on_stop:  function(ctx, reason) { if (Animus_Core.is_callable(self.stop)) self.stop(ctx, reason); }
});
```
</details>

**GOAP/scripts/Animus_Debug/Animus_Debug.gml** -> `timed`
<details><summary>Suggested template code</summary>

```gml
return Animus_StrategyTemplates.timed({
    expected_duration: (Animus_Core.is_callable(self.get_expected_duration) ? self.get_expected_duration() : 0.6),
    timeout: undefined,
    on_start: function(ctx) { if (Animus_Core.is_callable(self.start)) self.start(ctx); },
    on_stop:  function(ctx, reason) { if (Animus_Core.is_callable(self.stop)) self.stop(ctx, reason); }
});
```
</details>

**GOAP/scripts/Animus_Executor/Animus_Executor.gml** -> `timed`
<details><summary>Suggested template code</summary>

```gml
return Animus_StrategyTemplates.timed({
    expected_duration: (Animus_Core.is_callable(self.get_expected_duration) ? self.get_expected_duration() : 0.6),
    timeout: undefined,
    on_start: function(ctx) { if (Animus_Core.is_callable(self.start)) self.start(ctx); },
    on_stop:  function(ctx, reason) { if (Animus_Core.is_callable(self.stop)) self.stop(ctx, reason); }
});
```
</details>

**GOAP/scripts/Animus_Goal/Animus_Goal.gml** -> `timed`
<details><summary>Suggested template code</summary>

```gml
return Animus_StrategyTemplates.timed({
    expected_duration: (Animus_Core.is_callable(self.get_expected_duration) ? self.get_expected_duration() : 0.6),
    timeout: undefined,
    on_start: function(ctx) { if (Animus_Core.is_callable(self.start)) self.start(ctx); },
    on_stop:  function(ctx, reason) { if (Animus_Core.is_callable(self.stop)) self.stop(ctx, reason); }
});
```
</details>

**GOAP/scripts/Animus_Memory/Animus_Memory.gml** -> `timed`
<details><summary>Suggested template code</summary>

```gml
return Animus_StrategyTemplates.timed({
    expected_duration: (Animus_Core.is_callable(self.get_expected_duration) ? self.get_expected_duration() : 0.6),
    timeout: undefined,
    on_start: function(ctx) { if (Animus_Core.is_callable(self.start)) self.start(ctx); },
    on_stop:  function(ctx, reason) { if (Animus_Core.is_callable(self.stop)) self.stop(ctx, reason); }
});
```
</details>

**GOAP/scripts/Animus_Plan/Animus_Plan.gml** -> `timed`
<details><summary>Suggested template code</summary>

```gml
return Animus_StrategyTemplates.timed({
    expected_duration: (Animus_Core.is_callable(self.get_expected_duration) ? self.get_expected_duration() : 0.6),
    timeout: undefined,
    on_start: function(ctx) { if (Animus_Core.is_callable(self.start)) self.start(ctx); },
    on_stop:  function(ctx, reason) { if (Animus_Core.is_callable(self.stop)) self.stop(ctx, reason); }
});
```
</details>

**GOAP/scripts/Animus_Planner/Animus_Planner.gml** -> `timed`
<details><summary>Suggested template code</summary>

```gml
return Animus_StrategyTemplates.timed({
    expected_duration: (Animus_Core.is_callable(self.get_expected_duration) ? self.get_expected_duration() : 0.6),
    timeout: undefined,
    on_start: function(ctx) { if (Animus_Core.is_callable(self.start)) self.start(ctx); },
    on_stop:  function(ctx, reason) { if (Animus_Core.is_callable(self.stop)) self.stop(ctx, reason); }
});
```
</details>

**GOAP/scripts/Animus_Predicate/Animus_Predicate.gml** -> `timed`
<details><summary>Suggested template code</summary>

```gml
return Animus_StrategyTemplates.timed({
    expected_duration: (Animus_Core.is_callable(self.get_expected_duration) ? self.get_expected_duration() : 0.6),
    timeout: undefined,
    on_start: function(ctx) { if (Animus_Core.is_callable(self.start)) self.start(ctx); },
    on_stop:  function(ctx, reason) { if (Animus_Core.is_callable(self.stop)) self.stop(ctx, reason); }
});
```
</details>

**GOAP/scripts/Animus_RunState/Animus_RunState.gml** -> `timed`
<details><summary>Suggested template code</summary>

```gml
return Animus_StrategyTemplates.timed({
    expected_duration: (Animus_Core.is_callable(self.get_expected_duration) ? self.get_expected_duration() : 0.6),
    timeout: undefined,
    on_start: function(ctx) { if (Animus_Core.is_callable(self.start)) self.start(ctx); },
    on_stop:  function(ctx, reason) { if (Animus_Core.is_callable(self.stop)) self.stop(ctx, reason); }
});
```
</details>

**GOAP/scripts/Animus_Sensor/Animus_Sensor.gml** -> `timed`
<details><summary>Suggested template code</summary>

```gml
return Animus_StrategyTemplates.timed({
    expected_duration: (Animus_Core.is_callable(self.get_expected_duration) ? self.get_expected_duration() : 0.6),
    timeout: undefined,
    on_start: function(ctx) { if (Animus_Core.is_callable(self.start)) self.start(ctx); },
    on_stop:  function(ctx, reason) { if (Animus_Core.is_callable(self.stop)) self.stop(ctx, reason); }
});
```
</details>

**GOAP/scripts/Animus_SensorHub/Animus_SensorHub.gml** -> `timed`
<details><summary>Suggested template code</summary>

```gml
return Animus_StrategyTemplates.timed({
    expected_duration: (Animus_Core.is_callable(self.get_expected_duration) ? self.get_expected_duration() : 0.6),
    timeout: undefined,
    on_start: function(ctx) { if (Animus_Core.is_callable(self.start)) self.start(ctx); },
    on_stop:  function(ctx, reason) { if (Animus_Core.is_callable(self.stop)) self.stop(ctx, reason); }
});
```
</details>
